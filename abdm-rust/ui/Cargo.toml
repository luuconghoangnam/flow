[package]
name = "ui"
version = "0.1.0"
edition = "2021"

[dependencies]
core = { path = "../core" }
messaging = { path = "../messaging" }
slint = "1.6.0"
tokio = { version = "1.37", features = ["full"] }

[build-dependencies]
slint-build = "1.6.0"
