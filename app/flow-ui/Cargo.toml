[package]
name = "flow-ui"
version = "0.1.0"
edition = "2021"

[dependencies]
flow-core = { path = "../flow-core" }
flow-messaging = { path = "../flow-messaging" }
slint = "1.6.0"
tokio = { version = "1.37", features = ["full"] }

[build-dependencies]
slint-build = "1.6.0"
